export class Note {
    constructor(
        public id?: number,
        public text?: string,
        public colour?: string,
        public left?: number,
        public top?: number,
    ) {
    }
}
